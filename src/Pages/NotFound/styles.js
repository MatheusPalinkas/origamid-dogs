import styled from "styled-components";

import { Container as ContainerDad, Title as TitleDad } from "../../styles";

export const Container = styled(ContainerDad)`
  margin-top: 2rem;
  animation: none;
`;

export const Title = styled(TitleDad)`
  margin: 2rem 0;
`;

export const Text = styled.p``;
